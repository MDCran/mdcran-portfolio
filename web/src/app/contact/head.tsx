import { SeoHead } from "@/lib/seo";

export default function Head() {
  return (
    <SeoHead
      title="Contact"
      description="Contact MDCran to discuss projects, timelines, collaborations, and custom work."
      path="/contact"
    />
  );
}
